//Quarks used:
Quarks.gui

Quarks.install( "SenseWorld" );  // SWPlotterMonitor
Quarks.install( "DataNetwork" ); // data hub
Quarks.install( "adclib" );      // MFunc

// and add the folder 'classes' to your setup (for MData, MFilter, MThresholdTrigger)
